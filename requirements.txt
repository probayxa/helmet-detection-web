# ==== Minimal requirements for Helmet Detection Streamlit app on Streamlit Cloud ====

# Web UI
streamlit==1.38.0

# YOLO + dependencies (sẽ tự kéo torch, torchvision phù hợp)
ultralytics==8.3.0

# OpenCV bản headless để tránh lỗi libGL trên server
opencv-python-headless==4.8.1.78

# Các thư viện phụ trợ
numpy==1.26.4
pandas==2.2.3
pillow==10.4.0
matplotlib==3.9.2
seaborn==0.13.2
tqdm==4.66.5
pyyaml==6.0.2
requests==2.32.3
scipy==1.14.1
